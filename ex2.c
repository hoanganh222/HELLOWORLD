#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int dola,vnd,tygia;
	tygia =24000;
	scanf("%d",&dola);
	vnd= dola*tygia;
	printf("doi ra la %d vnd",vnd);
	return 0;
}
